[package]
name = "hk-plugins"
description = "hackriff decoder plugin host, manifest and IPC (ADR-0003)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

# `src/bin/hk-dummy-plugin.rs` is the test plugin (T-014). It is a bin target rather than an
# example so integration tests can locate it through `CARGO_BIN_EXE_hk-dummy-plugin`.

[dependencies]
hk-api.workspace = true
hk-model.workspace = true
libc.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
