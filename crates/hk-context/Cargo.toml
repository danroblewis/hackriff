[package]
name = "hk-context"
description = "hackriff external-feed cache, priors lookup and event correlation"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
hk-model.workspace = true
# T-020: FloorEvent (C08) → Anomaly lifecycle.
hk-dsp.workspace = true
h3o.workspace = true
# T-113 (ADR-0012 §11): occupancy engine, baselines and reports read/write hk-store series.
hk-store.workspace = true
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
thiserror.workspace = true
uuid.workspace = true

[dev-dependencies]
# T-020: AWARE-006 end-to-end (synth → STFT → NoiseFloorTracker → Anomaly → Explanation).
hk-core.workspace = true
hk-e2e = { path = "../../tests/e2e" }
num-complex.workspace = true
# T-201: the C18 blind signature test measures a generated emission with the classifier and the
# `features@1` vector, then matches only what it measured. Dev-only: nothing in hk-context's build
# depends on the classifier.
hk-classify.workspace = true
