[package]
name = "s1-fsdr"
version = "0.1.0"
edition = "2024"
publish = false

# Standalone spike: never join the repo's root workspace.
[workspace]

[dependencies]
futuresdr = { version = "=0.8.0", default-features = false }
anyhow = "1"
libc = "0.2"
num-complex = "0.4"

[profile.release]
debug = 1
codegen-units = 1
lto = "thin"
