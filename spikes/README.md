# spikes/ — throwaway spike code

Code for spikes S1–S7 (`docs/09`), one directory each (e.g. `spikes/s1-live-reconfig/`). It is not
product code and is excluded from the Cargo workspace, so a spike may carry its own `Cargo.toml`.
Record each spike's result in `docs/09` / the ADR it unblocks, not here.
