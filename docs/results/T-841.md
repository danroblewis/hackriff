T-841: decided (a) — ONE WRITER. Only the coordinator allocates ticket ids.

Changed (docs/rules only, no code):
- CLAUDE.md "Coordination": new bullet stating the rule, the 2026-09-22 evidence (five collisions,
  largest 26 wide, guard fires only post-merge), that non-coordinator sessions describe the ticket in
  prose with evidence in their result/handback (reusing the existing worker filing rule — no new
  tooling, no request queue), that amending an existing ticket needs no allocation, that the
  disjoint-range convention is the same rule, and the evidence that no collision came from an AGENT.
  Sub-bullets: the coordinator allocates over main's board plus its own in-flight branch tips; and
  plainly what still fails — two concurrent COORDINATOR sessions re-open the race (don't run two), a
  ticket living only in an unread result file is lost (visible and recoverable, unlike a duplicate id),
  and filing directly on main from a non-coordinator session is now banned.
- .claude/skills/file-ticket/SKILL.md §3: only the coordinator allocates; max+1 is computed over
  main's board + every branch tip it has out, never one worktree's copy.
- .claude/roles/coordinator.md: "you are the only ticket-id allocator" + how to compute it.
- .claude/roles/supervisor.md: does not allocate ids; hands text to the coordinator.

Why (a) over (b): (b) is still racy in the same instant and wrong when a branch is deleted between
allocation and merge; (a) removes the race outright and needs no code. Rejected building a
request-queue file (user rule: minimal changes, no meta-workflows) — the result/handback channel
already exists and already carries exactly this.

Tests: docs/rules-only diff, no crates and no py/ code touched, so no targeted suite applies and
`just precheck` has no crate argument. py/tests/test_task_board.py is unchanged and still the
post-merge backstop.

Surfaced, not chased: the brief told me to record this with `just task result T-841 --from <file>`,
but no `task` recipe exists in the justfile on this branch or on main (`just --list | grep task` is
empty), so the report is committed as docs/results/T-841.md instead. Also: the guard remains
post-merge only; a pre-merge check (reject a board whose new ids already exist on main) is possible
but was out of scope here.
